pipeline {
    agent any
    stages {
        stage('build-docker-base') {
            steps {
                sh 'docker build -f Dockerfile_base -t kristianskokars/ubuntu_ruby_base:latest .'
            }   
        }
    }
}