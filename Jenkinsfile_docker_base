pipeline {
    agent any
    stages {
        stage('build-docker-base') {
            steps {
                sh -c 'docker build -f Dockerfile_base -t kristianskokars/ubuntu_ruby_base:latest .'
            }   
        }
    }
}